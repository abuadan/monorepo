[package]
name = "sql_expr"
version = "0.1.0"
edition = "2024"

[dependencies]
sql_core = { path = "../sql_core" }
sql_macro = { path = "../sql_macro" }
chumsky = { version = "1.0.0-alpha.8", features = ["pratt"] }
